<% if form.errors.any? %>
  <div class="alert alert-danger">
    <h4>Sorry, there are errors.</h4>
    <ul>
      <% form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
